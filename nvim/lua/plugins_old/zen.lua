return {
  -- Using my own fork until this gets merged in:
  -- https://github.com/folke/zen-mode.nvim/pull/119/commits
  "davidpeach/zen-mode.nvim",
  opts = {
    window = {
      width = .5,
      backdrop = 1,
    },
  }
}

