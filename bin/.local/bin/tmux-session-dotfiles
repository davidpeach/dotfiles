#!/bin/bash

session="Dotfiles"
tmux has-session -t $session 2>/dev/null

if [ $? != 0 ]; then
    tmux new -s $session -d
    tmux rename-window -t $session Dotfiles
    tmux send-keys -t $session 'nvim ~/.dotfiles/' C-m
fi

tmux attach -t $session
